program Logicals is

global bool x;
global char c;

begin

getBool(x);
if(x) then
	putChar('T') // Missing ';' line 10
else
	putChar('F');
end if;

getChar(c);
if(c == 'A') // Missing 'then' line 16
	putChar('T');
else
	putChar('F');
end fi; // Mispelt 'if' line 20

end program.
